{% extends 'base.html.twig' %}

{% block body %}
    <h1>Article edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('article_show', {'id': article.id, 'titre': article.slug }) }}">Retour à l'article</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" onclick="return confirm('Voulez-vous vraiment supprimer cet article ?')">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
