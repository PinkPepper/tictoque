{% extends 'backoffice/menuAdmin.html.twig' %}

{% block body %}
    <section>
        <h1>Modifier une allergène</h1>
        <a class="btn_01" href="{{path('admin_allergene_index')}}">Retour</a>
        <div class="cellule_background_cms">
            {{ form_start(edit_form) }}

            <label>Nom</label>
            {{ form_widget(edit_form.nom) }}
            {{ form_errors(edit_form.nom) }}

            <button class="button_p" type="submit">Modifier la allergène</button>

            {{ form_end(edit_form) }}
        </div>
    </section>
{% endblock %}
