{% extends 'frontoffice/footer.html.twig' %}

{% block body %}
<style type="text/css">.noPadding{
	padding: 0px;
	margin-top: 40px;
	}</style>
<section>
	<div>
		{% if app.user is null %}<a class="noPadding" href="{{ path('fos_user_security_login') }}"><h2>Connexion</h2></a>{% endif %}
	</div>
	<div>
		{% if app.user is null %}<a class="noPadding" href="{{ path('fos_user_registration_register') }}"><h2>S'inscrire</h2></a>{% endif %}
	</div>
	<div>
		{% if is_granted('ROLE_ADMIN') %}<a class="noPadding" href="{{ path('admin_index') }}"><h2>Admin backoffice</h2></a>{% endif %}
	</div>
	<div>
	    {% if app.user is not null %}<a class="noPadding" href="{{ path('fos_user_profile_show') }}"><h2>Profile</h2>{% endif %}</a>
	</div>
	<div>
		<a class="noPadding" href="{{ path('blog_index') }}"><h2>Blog</h2></a>
	</div>
	<div>
		<a class="noPadding" href="{{ path('categorie_index') }}"><h2>Les catégories et leurs produits</h2></a>
	</div>
	<div>
	    {% if app.user is not null %} <a class="noPadding" href="{{ path('fos_user_security_logout') }}"> <h2>Déconnexion</h2>{% endif %}</a>
	</div>
</section>
{% endblock %}
