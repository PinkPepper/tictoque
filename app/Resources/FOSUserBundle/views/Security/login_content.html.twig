{% trans_default_domain 'FOSUserBundle' %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}

<section>
    <img src="{{ asset('img/logo_CMS.png') }}">
    <div id="login">
        <form action="{{ path("fos_user_security_check") }}" method="post">
            {% if csrf_token %}
                <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
            {% endif %}
            <div>
                <label for="username">Identifiant</label>
                <input type="text" id="username" name="_username" value="{{ last_username }}" required="required" />
            </div>

            <div>
                <label for="password">{{ 'security.login.password'|trans }}</label>
                <input type="password" id="password" name="_password" required="required" />
            </div>

            <button type="submit" id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}">Envoyer</button>
        </form>
    </div>
    <a href="mdp.html" id="password_recover">Mot de passe oublié ?</a>
</section>

<!-- <section>

            <img src="img/logo_CMS.png">

            <div id="login">
                <div>
                    <label>Identifiant</label>
                    <input type="email" required></input>
                </div>

                <div>
                    <label>Mot de passe</label>
                    <input type="password" required></input>
                </div>

                <div class="error">
                    <p></p>
                </div>

                <button>SE CONNECTER</button>
            </div>
            
            <a href="mdp.html" id="password_recover">Mot de passe oublié ?</a>

        </section> -->
